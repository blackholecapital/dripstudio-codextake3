{
  "name": "@gateway/editor-web",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "build:worker": "opennextjs-cloudflare build",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "deploy:staging": "npm run build:worker && wrangler deploy --dry-run --env staging",
    "preview:worker": "npm run build:worker && wrangler dev --env staging --local --port 8788"
  }
}
